[package]
name = "noye-storage-app"
version = "0.1.0"
edition = "2024"

[dependencies]
noye-const = { path = "../../const" }

sea-orm = { version = "2.0.0-rc", features = [ "runtime-tokio", "sqlx-sqlite" ] }
