[package]
name = "noye-storage"
version = "0.1.0"
edition = "2024"

[dependencies]
noye-const = { path = "../const" }

sqlx = { version = "0", default-features = false, features = ["runtime-tokio", "sqlite"] }
